# Система управления администраторами

## Обзор

Теперь ваше приложение поддерживает несколько учетных записей администраторов с разными уровнями доступа.

## Роли администраторов

### 1. Супер-администратор (super_admin)
- **Полный доступ** ко всем функциям
- Управление учетными записями администраторов
- Редактирование мануалов и тематик
- Просмотр статистики

### 2. Редактор (editor)
- Редактирование мануалов
- Управление тематиками
- Просмотр статистики
- **НЕ может** управлять учетными записями

## Начало работы

### Миграция существующей учетной записи

Ваша существующая учетная запись уже мигрирована в новую систему!

```bash
# Файл admins.json содержит:
# - Логин: Helperinio
# - Роль: Супер-администратор
```

### Вход в систему

1. Откройте админ-панель: `http://localhost:5003/admin/login`
2. Войдите используя существующие учетные данные
3. Теперь у вас будет кнопка **"👥 Администраторы"** в верхнем меню

## Управление администраторами

### Добавление нового администратора

1. Войдите как супер-администратор
2. Нажмите **"👥 Администраторы"**
3. Нажмите **"➕ Добавить администратора"**
4. Заполните форму:
   - **Логин**: 3-30 символов (латиница, цифры, подчеркивание)
   - **Пароль**: минимум 6 символов
   - **Роль**: выберите уровень доступа

### Изменение пароля

1. В списке администраторов нажмите **"🔑 Пароль"**
2. Введите новый пароль
3. Подтвердите изменения

### Изменение роли

- В списке администраторов выберите новую роль из выпадающего списка
- Роль изменится автоматически
- **Важно**: Нельзя понизить роль последнего супер-администратора

### Удаление администратора

1. Нажмите **"🗑️ Удалить"** в списке администраторов
2. Подтвердите удаление
- **Важно**: Нельзя удалить последнего супер-администратора
- **Важно**: Нельзя удалить самого себя

## Хранение данных

### Файл admins.json

Учетные записи хранятся в файле `admins.json`:

```json
{
  "admins": [
    {
      "username": "admin1",
      "password_hash": "scrypt:...",
      "role": "super_admin",
      "created_by": "system",
      "created_at": "2025-12-11 10:00:00",
      "active": true
    }
  ]
}
```

**⚠️ ВАЖНО**:
- Файл `admins.json` автоматически добавлен в `.gitignore`
- Не коммитьте этот файл в Git!
- Храните резервные копии в безопасном месте

## Обратная совместимость

Система поддерживает старый способ аутентификации через `.env`:

1. Сначала проверяется `admins.json`
2. Если пользователь не найден, проверяется `.env` (ADMIN_USERNAME, ADMIN_PASSWORD_HASH)

Вы можете оставить старые переменные в `.env` для резервного доступа.

## Безопасность

### Рекомендации:

1. **Используйте сильные пароли** (минимум 12 символов)
2. **Регулярно меняйте пароли** администраторов
3. **Не передавайте** пароли в открытом виде
4. **Создавайте отдельные учетки** для каждого сотрудника
5. **Используйте роль "Редактор"** для сотрудников, которым не нужны все права

### Защита от атак:

- ✅ Rate limiting на логин (5 попыток за 15 минут)
- ✅ Хеширование паролей (scrypt)
- ✅ Валидация входных данных
- ✅ Защита от SQL-инъекций
- ✅ CSRF защита

## Примеры использования

### Создание администратора для контент-менеджера

```
Логин: content_manager
Пароль: SecurePass123!
Роль: Редактор
```

Этот пользователь сможет редактировать мануалы, но не управлять учетками.

### Создание второго супер-админа

```
Логин: super_admin_2
Пароль: VerySecurePass456!
Роль: Супер-администратор
```

Теперь у вас есть резервный супер-админ на случай проблем с основной учеткой.

## Устранение проблем

### Забыли пароль супер-админа?

Если все супер-админы потеряли доступ:

1. Откройте `.env` файл
2. Добавьте/раскомментируйте строки:
   ```
   ADMIN_USERNAME=emergency_admin
   ADMIN_PASSWORD_HASH=<хеш_нового_пароля>
   ```
3. Перезапустите приложение
4. Войдите используя аварийную учетку

### Генерация хеша пароля

```python
from werkzeug.security import generate_password_hash

password = "your_password"
hash = generate_password_hash(password)
print(hash)
```

## Дополнительная информация

- Файлы: `admin_manager.py`, `helper7.py`
- HTML шаблоны: `templates/admin_users.html`, `templates/admin_add_user.html`
- Миграция: `migrate_admin.py`

---

**Поддержка**: Если возникли проблемы, проверьте логи приложения или обратитесь к разработчику.
